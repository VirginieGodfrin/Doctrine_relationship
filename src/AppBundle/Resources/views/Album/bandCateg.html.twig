{% extends "AppBundle::layout.html.twig" %}

{% block bandCateg %}

		

{{ dump(bandCateg) }}


	{% for bc in bandCateg %}

		<ul>
			<li>{{bandCateg.id}}</li>
			<li>{{bandCateg.name}}</li>
			<li>{{bandCateg.band.name}}</li>
		</ul>
		
		
	{% endfor %}

	{% endblock %}