{% extends "AppBundle:Admin:adminLayout.html.twig" %}

{% block eventEdit %}

	<h3>Event Edit</h3>
	
	{{ form_start(eventForm) }}
	{{ form_widget(eventForm) }}
	<input type="submit" value="Modifier l'évent"/>
	{{ form_end(eventForm) }}

	{% endblock %}