{{ form_start(albumForm) }}
	{{ form_row(albumForm.name) }}
	{{ form_row(albumForm.description) }}
	{{ form_row(albumForm.isPublish) }}
	{{ form_row(albumForm.band) }}
	<input type="submit" value="submit" formnovalidate/>
{{ form_end(albumForm) }}