{% extends "AppBundle:Admin:adminLayout.html.twig" %}

{% block bandEdit %}

	<h3>Band Edit </h3>

	{{ form_start(bandForm) }}
	{{ form_widget(bandForm) }}
	<input type="submit" value="Modifier le band"/>
	{{ form_end(bandForm) }}

	{% endblock %}