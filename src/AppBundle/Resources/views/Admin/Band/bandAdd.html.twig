{% extends "AppBundle:Admin:adminLayout.html.twig" %}

{% block bandAdd %}

	<h3>Band add</h3>

	{{ form_start(bandForm) }}
	{{ form_widget(bandForm) }}
	<input type="submit" value="Ajouter un band"/>
	{{ form_end(bandForm) }}

	{% endblock %}